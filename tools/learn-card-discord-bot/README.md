### LearnCard Discord Bot 

To get started:


Add a `.env` file to root of project, and include the following keys:

```
DISCORD_TOKEN=<TOKEN>
SEED=<WALLET SEED>
```

Run:

```
pnpm build
pnpm start 
```
