---
'@learncard/didkit-plugin': patch
'@learncard/vc-plugin': patch
---

Add OBv3 Data Integrity proof issuance using `eddsa-rdfc-2022` by default, while allowing callers to select a Data Integrity `cryptosuite` through proof options.
