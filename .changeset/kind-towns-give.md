---
'@learncard/didkit-plugin': patch
---

Add W3C Bitstring Status List support to DIDKit credential verification. Credentials that use `BitstringStatusListEntry` can now be checked against `BitstringStatusListCredential` status credentials, including revoked and unrevoked VC 2.0 status entries.
