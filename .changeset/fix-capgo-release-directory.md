---
'learn-card-app': patch
---

Restore CapGo OTA releases by running the uploader from the app directory with correctly resolved bundle and dependency paths. Pin the CapGo CLI version for production, staging, and preview uploads.
