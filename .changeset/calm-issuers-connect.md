---
"@learncard/network-brain-service": minor
"@learncard/network-plugin": minor
"@learncard/react": major
"@learncard/types": minor
"learn-card-app": patch
"learn-card-base": patch
"scoutpass-app": patch
---

Resolve credential issuers through direct and mutual connections or verified profile details, and display that relationship-aware context consistently across LearnCard credential cards. Credential card consumers now pass `issuerContext` and `issuerLabel` instead of the previous registry and unknown-issuer props.
