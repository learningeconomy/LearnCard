---
'@learncard/ai-agent-service': patch
---

Restore the compatible ohash dependency for the Trigger.dev deployment CLI and check CLI startup during PR and deployment validation, preventing dependency import failures from reaching the production deployment step.
