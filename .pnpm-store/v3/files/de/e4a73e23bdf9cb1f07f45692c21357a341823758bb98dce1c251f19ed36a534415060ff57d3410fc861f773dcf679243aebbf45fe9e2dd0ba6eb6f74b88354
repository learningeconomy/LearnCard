import { PointerTarget, SelectionTarget } from './types';
export declare function resolveSelectionTarget({ target, node, offset, }: PointerTarget & SelectionTarget): {
    node: Node;
    offset: number;
};
