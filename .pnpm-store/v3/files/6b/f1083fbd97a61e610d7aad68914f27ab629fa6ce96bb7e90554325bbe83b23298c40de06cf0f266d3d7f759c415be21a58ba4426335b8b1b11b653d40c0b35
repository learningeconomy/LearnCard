import React from "react";
import { render } from "react-dom";

import { Berries } from "${name}";

render(
  <React.StrictMode>
    <Berries name="${author}" />
  </React.StrictMode>,
  document.getElementById("root")
);
