name: Dispatch DIDKit update to PR 1444

on:
    push:
        branches:
            - lc-1958-eddsa-data-integrity-interop
        paths:
            - '.github/workflows/dispatch-didkit-update-1444.yml'

permissions:
    actions: write
    contents: read

jobs:
    dispatch:
        runs-on: ubuntu-latest
        steps:
            - name: Dispatch existing DIDKit publisher on the PR branch
              uses: actions/github-script@v7
              with:
                  github-token: ${{ secrets.GITHUB_TOKEN }}
                  script: |
                      await github.rest.actions.createWorkflowDispatch({
                          owner: context.repo.owner,
                          repo: context.repo.repo,
                          workflow_id: 'update-didkit-wasm.yml',
                          ref: 'lc-1958-eddsa-data-integrity-interop',
                          inputs: {
                              didkit_ref: 'main',
                              ssi_ref: 'main',
                              force_rebuild: 'false',
                          },
                      });
