name: DIDKit update helper for PR 1444 (disabled)

on:
    pull_request:
        branches:
            - main

jobs:
    disabled:
        if: false
        runs-on: ubuntu-latest
        steps:
            - run: echo disabled
