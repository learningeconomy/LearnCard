import { environment } from '@environment';
/** XAPI Endpoint. */
export const XAPI_ENDPOINT = environment.XAPI_ENDPOINT;

/** Extension IRI for contract URI in xAPI statements. */
export const XAPI_CONTRACT_URI_EXTENSION = 'https://learncard.com/xapi/extensions/contractUri';
