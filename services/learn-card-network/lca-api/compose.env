# Local Docker defaults only; encryption is opt-in and requires an explicit KEK.
# Load before optional .env files so existing custom keys and rollout settings win.
# For persistent data, configure a unique KEK before the first encrypted write and retain it.
SA_SEED_ENCRYPT_WRITES=false
SA_SEED_ALLOW_LEGACY_READ=true
