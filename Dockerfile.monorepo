# syntax=docker/dockerfile:1.7
FROM oven/bun:1.3.14

ENV SKIP_DIDKIT_NAPI=1

WORKDIR /app

COPY . .

# Cache dependencies, making builds after the first faster
RUN --mount=type=cache,target=/root/.bun/install/cache \
    bun install
