export enum DisplayTypeEnum {
    Badge = 'badge',
    Certificate = 'certificate',
    ID = 'id',
    Course = 'course',
    Award = 'award',
    Media = 'media',
}

export enum PreviewTypeEnum {
    Default = 'default',
    Media = 'media',
}
