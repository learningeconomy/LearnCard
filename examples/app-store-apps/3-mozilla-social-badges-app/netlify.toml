[build.environment]
BUN_VERSION = "1.3.14"
SKIP_DIDKIT_NAPI = "1"

[build]
command = "bunx nx run @learncard/app-store-demo-mozilla-social-badges:build"
  publish = "dist"

[context.production.environment]
  NODE_VERSION = "20"

[context.deploy-preview.environment]
  NODE_VERSION = "20"

[context.branch-deploy.environment]
  NODE_VERSION = "20"
