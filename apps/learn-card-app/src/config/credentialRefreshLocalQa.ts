/** Refresh transport and Boost trust use the same development-only loopback gate. */
export { getLocalDevelopmentNetworkOrigin as getCredentialRefreshLocalQaOrigin } from 'learn-card-base/config/localDevelopmentNetwork';
